apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-argo-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: nginx-argo-app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx-argo-app-service
            port:
              number: 80